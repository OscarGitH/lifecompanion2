export const SCHEMA_NAME_SYMBOL = Symbol();

export const RELATION_SYMBOL = Symbol();

export const RELATION_SCHEMA_SYMBOL = Symbol();

export const BELONGS_TO_SYMBOL = Symbol();

export const HAS_MANY_SYMBOL = Symbol();

export const SCHEMA_RECORD_SCHEMA_SYMBOL = Symbol();

export const SCHEMA_RECORD_VALUES_SYMBOL = Symbol();
