name: setup_build
description: Setup common LifeCompanion build environment

inputs:
  tauri-signing-private-key:
    description: Tauri signing private key
    required: true
  tauri-signing-private-key-password:
    description: Tauri signing password
    required: true

runs:
  using: composite
  steps:
    - name: Set Docker UID/GID
      shell: bash
      run: |
        echo "UID=$(id -u)" >> "$GITHUB_ENV"
        echo "GID=$(id -g)" >> "$GITHUB_ENV"

    - name: Setup Tauri signing
      shell: bash
      env:
        TAURI_SIGNING_PRIVATE_KEY: ${{ inputs.tauri-signing-private-key }}
        TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ inputs.tauri-signing-private-key-password }}
      run: |
        echo "Private key set: $([[ -n "$TAURI_SIGNING_PRIVATE_KEY" ]] && echo yes || echo no)"
        echo "Private key length: ${#TAURI_SIGNING_PRIVATE_KEY}"
        echo "Password set: $([[ -n "$TAURI_SIGNING_PRIVATE_KEY_PASSWORD" ]] && echo yes || echo no)"
        echo "Password length: ${#TAURI_SIGNING_PRIVATE_KEY_PASSWORD}"
        
        cat > .env <<EOF
        TAURI_SIGNING_PRIVATE_KEY=$TAURI_SIGNING_PRIVATE_KEY
        TAURI_SIGNING_PRIVATE_KEY_PASSWORD=$TAURI_SIGNING_PRIVATE_KEY_PASSWORD
        EOF